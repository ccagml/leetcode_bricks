{"data":[{"name":"2023-10-26 11:30:36","id":1698291036228,"body":"class Solution\n{\npublic:\n    int minAbsoluteDifference(vector<int> &nums, int x)\n    {\n        if (x == 0)\n        {\n            return 0;\n        }\n        vector<int> wait;\n        // for (int i = nums.size() - 1; i >= nums.size() - x; i--)\n        // {\n        //     wait.push_back(nums[i]);\n        // }\n        // sort(wait.begin(), wait.end());\n        // [0, 1, 2, 3]\n        int ans = abs(nums[nums.size() - 1] - nums[0]);\n        for (int start = nums.size() - 1 - x; start >= 0; start--)\n        {\n            if (start + x < nums.size())\n            {\n                wait.push_back(nums[start + x]);\n                sort(wait.begin(), wait.end());\n            }\n\n            int cur = nums[start];\n            int check_left = cur - ans + 1;\n            int check_right = cur + ans - 1;\n\n            // 第一个大于等于 i 的位置\n            auto lower = std::lower_bound(wait.begin(), wait.end(), check_left);\n\n            if (lower != wait.end())\n            {\n                // std::cout << *lower << \" at index \" << std::distance(data.begin(), lower);\n                for (int j = std::distance(wait.begin(), lower); j < wait.size(); j++)\n                {\n                    int cur_j = wait[j];\n                    if (cur_j > check_right)\n                    {\n                        break;\n                    }\n                    ans = min(abs(cur_j - cur), ans);\n                }\n            }\n        }\n        return ans;\n    }\n};超时"}]}