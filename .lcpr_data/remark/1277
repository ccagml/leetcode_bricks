{"data":[{"name":"2023-01-19 16:33:43","id":1674117223887,"body":"是脑筋急转弯,不是dp\n\n2023-01-19 16:10:09\ndfs半小时 超时\nclass Solution\n{\n    string result = \"\";\n    bool check(std::string &aa, std::string &bb)\n    {\n        if (aa.size() > bb.size())\n        {\n            return true;\n        }\n        else if (bb.size() > aa.size())\n        {\n            return false;\n        }\n        for (int i = 0; i < aa.size(); i++)\n        {\n            if (aa[i] > bb[i])\n            {\n                return true;\n            }\n            else if (aa[i] < bb[i])\n            {\n                return false;\n            }\n        }\n        return false;\n    }\n\n    void dfs(int cur, std::string &cur_result, int cur_sum, vector<int> &digits)\n    {\n        if (cur >= digits.size())\n        {\n            return;\n        }\n        // 选\n\n        int new_cur_sum = cur_sum + digits[cur];\n        char next = '0' + digits[cur];\n        bool flag = false;\n        if (digits[cur] == 0 && cur_result.size() == 1 && cur_result[0] == '0')\n        {\n        }\n        else\n        {\n            flag = true;\n            cur_result.push_back(next);\n\n            if (new_cur_sum % 3 == 0 && check(cur_result, result))\n            {\n                // std::cout << \"new_cur_result:\" << cur_result << std::endl;\n                std::string temp(cur_result);\n                result = temp;\n            }\n        }\n        dfs(cur + 1, cur_result, new_cur_sum, digits);\n        if (flag)\n        {\n            cur_result.pop_back();\n        }\n\n        // 不选\n        dfs(cur + 1, cur_result, cur_sum, digits);\n    }\n\npublic:\n    string largestMultipleOfThree(vector<int> &digits)\n    {\n        // 每个数字 选和不选?\n        sort(digits.begin(), digits.end(), [](int a, int b)\n             { return a > b; });\n\n        string qqq;\n        dfs(0, qqq, 0, digits);\n        return result;\n    }\n};"},{"name":"2023-01-20 10:33:56","id":1674182036095,"body":"调了半小时\nAccepted\n42/42 cases passed (16 ms)\nYour runtime beats 73.81 % of cpp submissions\nYour memory usage beats 73.01 % of cpp submissions (18.9 MB)\n耗时0:22:52"}]}